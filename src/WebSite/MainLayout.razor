@using Microsoft.AspNetCore.Components.Authorization
@inherits LayoutComponentBase

<AuthorizeView>
    <Authorized>
        <img src="@context.User.Claims.Where(claim => claim.Type =="picture").FirstOrDefault()?.Value" />
        Hello @context.User.Identity.Name!  <a href="auth/google-logout">Logout</a>
    </Authorized>
    <NotAuthorized>
        <a href="auth/google-login">Login with google</a>
    </NotAuthorized>
</AuthorizeView>


<main> @Body </main>
